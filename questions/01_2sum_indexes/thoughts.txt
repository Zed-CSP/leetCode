hash map allows for O(n) complexity. 
left and right pointers were not viable as we recieved and unsorted array and an array of indicies were the values we needed to return.
only one value was needed to be returned.