function strStr(haystack, needle) {
    return haystack.indexOf(needle);
}

// .indexOf() method returns the index of the first occurrence of the substring needle in the string haystack. If the substring is not found, it returns -1.